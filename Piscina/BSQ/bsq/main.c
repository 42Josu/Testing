void anotardiccionario(char *dir)
{
	int	dicfile;
	
	dicfile = open(dir);
}

int main (int argn, char **str)
{
	char	**dic;

	anotardiccionario();
	return (1);
}

///open, close, read, write, malloc, free, exit
